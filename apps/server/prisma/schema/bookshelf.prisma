model BookShelf {
  id          Int      @id @default(autoincrement())
  label       String
  createTimer DateTime
  userId      Int
  user        User     @relation(fields: [userId], references: [id])
  books       Book[]
}
